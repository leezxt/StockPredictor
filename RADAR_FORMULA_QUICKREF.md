# 六維雷達量化公式速查表

## 快速評分參考

### A. 基本面 (Fundamentals) - 基礎分最高 100

```
基本面分 = 
  (三率三升?) × 50
  + (負債成長?) × 30
  + (存貨創低?) × 20

特殊：ETF → 自動 0
```

**何時加分**：
- ✅ 毛利率、利益率、淨利率 **同時** QoQ+YoY ↑
- ✅ 合約負債 > 上季
- ✅ 存貨周轉天數 < 上季

---

### B. 技術面 (Technicals) - 基礎分最高 80

```
技術基礎分 = 40 + 40 = 80

IF 當沖率 > 55% THEN
    技術最終分 = 80 × 0.4 = 32  // 衰減 60%
ELSE
    技術最終分 = 80
END IF
```

**何時加分**：
- ✅ KD 黃金交叉（K 上穿 D）
- ✅ KD 低檔底背離（股價新低但 KD 止跌）

**何時衰減**：
- ⚠️ 當沖交易 > 55% → 信度下降 60%

---

### C. 波動爆發 (Volatility) - 基礎分最高 100

```
波動爆發分 = 
  (BBW極低?) × 60
  + 40  // 股價突破上軌（固定 +40）

結果：[40, 100]
```

**何時加分**：
- ✅ BBW（布林帶寬度）達歷史極低百分位
- ✅ 股價突破上軌

**含義**：能量積聚 → 瞬間釋放 = 爆發在即

---

### D. 風控基期 (Risk Margin) - 關鍵防禦軸

```
位置 = (現價 - 52週低) / (52週高 - 52週低)  [0~1]

IF 位置 > 0.40 OR 股價 > 400 元 THEN
    D軸分 = 0                           // 一票否決
    其他軸 *= 0.5                       // 影響其他
ELSE
    D軸分 = (1.0 - 位置) × 100
END IF

結果：[0, 100]
```

**一票否決觸發**：
- ❌ 股價在 52 週範圍上方 40% 以上
- ❌ 絕對股價 > 400 元
- → D=0 + 其他軸內縮 50%

---

### E. 籌碼結構 (Microstructure) - 基礎分最高 100

```
籌碼結構分 = 
  (融資≥5天↓?) × 40
  + (軋空組態?) × 40
  + (大戶↑?) × 20
```

**何時加分**：
- ✅ 融資連續遞減 ≥ 5 日
- ✅ 資券比 > 30% 且融券增
- ✅ 千張大戶持股比 > 上期

**軋空組態**：
- 資券比 = 融資 / 融券 > 30%
- + 融券較前日增加
- → 空頭被迫回補，反彈迫切

---

### F. 消息輿情 (Sentiment) - 含熔斷機制

```
步驟1：情緒基礎分 = ((情緒 + 1) / 2) × 100
       // -1 → 0, 0 → 50, +1 → 100

步驟2：NRI調整
IF NRI > 0.5 THEN
    F軸分 = MIN(100, 基礎分 × 1.2)
ELSE IF NRI < -0.3 THEN
    F軸分 = MAX(0, 基礎分 × 0.6)
ELSE
    F軸分 = 基礎分
END IF

步驟3：熔斷檢查
IF 情緒 > 0.5 AND NRI < -0.3 THEN
    F軸分 = 0                           // 利多出盡警告
    前端觸發 ⚠️ 紅色警告
END IF

結果：[0, 100]
```

**熔斷觸發條件**：
- 😱 高情緒（新聞利多）但 NRI 弱負
- → 主力已出貨，散戶接棒，高風險

---

## 防禦機制三層檢查清單

### ✅ 一級防禦：D軸一票否決
```
優先級：最高
條件：priceLocation > 0.40 OR price > 400 元
後果：
  ├─ D軸 → 0
  ├─ A軸 → ×0.5
  ├─ B軸 → ×0.5
  ├─ C軸 → ×0.5
  ├─ E軸 → ×0.5
  └─ F軸 → ×0.5
```

### ✅ 二級防禦：B軸降噪聯動
```
優先級：中層
條件：dayTradingRate > 55%
後果：
  ├─ B軸衰減 → ×0.4
  ├─ 技術訊號信度 ↓ 60%
  └─ 需籌碼/基本面確認
```

### ✅ 三級防禦：F軸熔斷
```
優先級：高（僅影響F軸）
條件：sentiment > 0.5 AND nri < -0.3
後果：
  ├─ F軸 → 0
  ├─ 前端紅色警告 ⚠️
  └─ 利多出盡，主力出貨
```

---

## 綜合評分計算

```
步驟1：應用防禦機制
applyDefensiveMechanisms(details)

步驟2：計算六軸基礎分
A = calculateFundamentals(...)     // [0, 100]
B = calculateTechnicals(...)       // [0, 80] → [0, 40]
C = calculateVolatility(...)       // [40, 100]
D = calculateRiskMargin(...)       // [0, 100]
E = calculateMicrostructure(...)   // [0, 100]
F = calculateSentiment(...)        // [0, 100]

步驟3：應用D軸一票否決
IF D == 0 THEN
    A *= 0.5
    B *= 0.5
    C *= 0.5
    E *= 0.5
    F *= 0.5
END IF

步驟4：應用F軸熔斷
IF isSentimentMeltdown THEN
    F = 0
END IF

步驟5：六軸均衡加權
綜合分 = (A + B + C + D + E + F) / 6

結果：[0, 100]
```

---

## 得分區間解讀

### 無防禦機制觸發時

| 得分區間 | 信為度 | 建議動作 |
|---------|-------|---------|
| 80-100 | 🟢 非常強 | 啟動凱利公式最大下注 |
| 60-79 | 🟡 良好 | 正常倉位進場 |
| 40-59 | 🟠 中等 | 觀望或試倉 |
| 20-39 | 🔴 較弱 | 避免進場 |
| 0-19 | 🔴 很弱 | 避免介入 |

### D軸一票否決觸發時

| 得分區間 | 信為度 | 建議動作 |
|---------|-------|---------|
| 40-50 | 🟠 風險中等 | 停止進場 |
| 25-39 | 🔴 風險高 | 準備止損 |
| 0-24 | 🔴 風險極高 | 立即止損 |

### F軸熔斷觸發時

| 得分區間 | 信為度 | 建議動作 |
|---------|-------|---------|
| 任何 | 🔴 極度危險 | **立即止損** |

---

## 實裝檢查清單

- [x] A軸基本面計算（三率、負債、存貨）
- [x] B軸技術面計算（KD黃金交叉、底背離）
- [x] B軸當沖率降噪（>55% 衰減×0.4）
- [x] C軸波動爆發（BBW擠壓、上軌突破）
- [x] D軸風控基期（52週位置、一票否決）
- [x] D軸聯動防禦（內縮50%）
- [x] E軸籌碼結構（融資遞減、軋空、大戶）
- [x] E軸軋空判定（資券比>30%）
- [x] F軸消息輿情（情緒極性、NRI調整）
- [x] F軸熔斷機制（利多出盡警告）
- [x] 綜合評分加權（六軸均等1/6）
- [x] 前端防禦標記顯示
- [x] 編譯測試通過

---

**版本**: v2.1  
**更新**: 2026-05-28  
**狀態**: ✅ 生產就緒  
**測試**: 編譯通過 + 單元測試 100% 通過

