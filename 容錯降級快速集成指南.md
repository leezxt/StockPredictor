# 🚀 容錯降級機制 - 快速集成指南

## 📋 已實裝的內容

### ✅ InstitutionalService.java 的增強

```java
// 新增方法 1: 每日籌碼資料（自動容錯）
public List<InstitutionalTrade> getDailyChipDataWithFallback(String symbol, String date)

// 新增方法 2: 格式轉換
private List<InstitutionalTrade> convertFinMindToInternalFormat(List<FinMindChipData> backupData)

// 改進方法 3: getRecentInstitutionalTrades() 
// 現已支持 TWSE 失敗時自動切換到 FinMind
```

### ✅ FinMindClient.java 的增強

```java
// 新增方法: 備援機制入口
public List<FinMindChipData> fetchChipDataBackup(String symbol, String date)
```

---

## 🎯 三種使用方式

### 方式 1️⃣: 取得特定日期的籌碼資料（推薦用於即時查詢）

```java
@RestController
@RequestMapping("/api/chip")
public class ChipDataController {
    
    @Autowired
    private InstitutionalService institutionalService;
    
    @GetMapping("/daily/{symbol}/{date}")
    public ResponseEntity<?> getDailyChipData(
            @PathVariable String symbol,
            @PathVariable String date) {
        try {
            List<InstitutionalTrade> data = institutionalService
                .getDailyChipDataWithFallback(symbol, date);
            
            if (data.isEmpty()) {
                return ResponseEntity.ok(Map.of(
                    "status", "no_data",
                    "message", "市場休市或數據源皆不可用"
                ));
            }
            return ResponseEntity.ok(Map.of(
                "status", "success",
                "data", data
            ));
        } catch (Exception e) {
            return ResponseEntity.status(500).body(Map.of(
                "status", "error",
                "message", e.getMessage()
            ));
        }
    }
}
```

**觸發場景**：
- 用戶手動查詢某日籌碼資料
- API 端點收到查詢請求

**日誌輸出**：
```
🔄 [方案A] 正在嘗試從 TWSE 抓取籌碼資料 [2330] @ 2024-05-19
✅ [方案A成功] TWSE 返回籌碼資料: trustBuy=1234567
```

---

### 方式 2️⃣: 取得最近 N 天的籌碼資料（推薦用於趨勢分析）

```java
@RestController
@RequestMapping("/api/analysis")
public class AnalysisController {
    
    @Autowired
    private InstitutionalService institutionalService;
    
    @GetMapping("/chip-trend/{symbol}")
    public ResponseEntity<?> getChipTrend(@PathVariable String symbol) {
        try {
            // 取得最近 30 天資料（自動容錯）
            List<InstitutionalTrade> history = institutionalService
                .getRecentInstitutionalTrades(symbol, 30);
            
            // 計算趨勢指標
            int score = institutionalService.calculateTrustLockScore(symbol);
            double ratio = institutionalService.getLockRatio(symbol);
            int continuousDays = institutionalService.getContinuousBuyDays(symbol);
            
            return ResponseEntity.ok(Map.of(
                "symbol", symbol,
                "history_count", history.size(),
                "lock_score", score,
                "lock_ratio_percent", ratio,
                "continuous_buy_days", continuousDays,
                "history", history
            ));
        } catch (Exception e) {
            return ResponseEntity.status(500).body(Map.of(
                "error", e.getMessage()
            ));
        }
    }
}
```

**觸發場景**：
- 分析股票籌碼趨勢
- 判斷投信是否鎖碼

**日誌輸出**：
```
🔄 嘗試從 TWSE 抓取 2330 的最近 30 日資料
✅ TWSE 成功返回 25 筆資料
✅ 成功轉換 X 筆 FinMind 資料為內部格式  // 如果需要備援
```

---

### 方式 3️⃣: 定時自動更新（推薦用於背景定時任務）

```java
@Service
public class DailyDataUpdateService {
    
    @Autowired
    private InstitutionalService institutionalService;
    
    /**
     * 每日下午收盤後自動更新籌碼資料
     */
    @Scheduled(cron = "0 0 15 * * ?")  // 每日 15:00
    public void updateDailyChipData() {
        List<String> focused_symbols = List.of("2330", "2454", "3402", "2412");
        String today = LocalDate.now().toString();
        
        System.out.println("🔄 開始每日籌碼資料更新...");
        
        for (String symbol : focused_symbols) {
            try {
                // 自動容錯，失敗時自動切換到 FinMind
                List<InstitutionalTrade> data = institutionalService
                    .getDailyChipDataWithFallback(symbol, today);
                
                if (!data.isEmpty()) {
                    System.out.println("✅ " + symbol + " 資料已更新 (來源: TWSE或FinMind)");
                } else {
                    System.out.println("⚠️  " + symbol + " 無法獲得資料 (市場休市或源都不可用)");
                }
            } catch (Exception e) {
                System.err.println("❌ " + symbol + " 更新失敗: " + e.getMessage());
            }
        }
        
        System.out.println("✅ 每日籌碼資料更新完成");
    }
}
```

**觸發時機**：
- 每日固定時間（例如下午3點收盤後）
- 自動批量更新籌碼資料到資料庫

**日誌輸出**：
```
🔄 開始每日籌碼資料更新...
🔄 處理 2330...
🔄 [方案A] 正在嘗試從 TWSE 抓取籌碼資料 [2330]...
❌ [方案A失敗] TWSE 伺服器報錯... SocketTimeoutException
🔄 [方案B] 正在嘗試從 FinMind API 抓取籌碼資料 [2330]...
✅ [方案B成功] FinMind API 返回 1 筆籌碼資料
✅ 2330 資料已更新 (來源: FinMind)
✅ 每日籌碼資料更新完成
```

---

## 🔧 配置檢查清單

在使用容錯降級機制前，確保以下配置已完成：

### ✅ 1. application.properties

```properties
# FinMind API 配置
finmind.api.token=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...
finmind.api.url=https://api.finmindtrade.com/api/v4/data
```

驗證方式：
```bash
# 在 Spring Boot 啟動時查看這些属性是否被加載
grep "finmind.api" 啟動日誌
```

### ✅ 2. AppConfig.java 中的 RestTemplate

```java
@Bean
public RestTemplate restTemplate(RestTemplateBuilder builder) {
    return builder
            .setConnectTimeout(Duration.ofSeconds(5))
            .setReadTimeout(Duration.ofSeconds(10))
            .build();
}
```

### ✅ 3. InstitutionalService 中的 @Autowired

```java
@Service
public class InstitutionalService {
    @Autowired
    private FinMindClient finMindClient;  // ✅ 已添加
    // ...
}
```

### ✅ 4. 資料庫連接（可選）

確保 DatabaseManager 能夠正常保存資料：
```java
DatabaseManager.saveInstitutionalTrades(symbol, fetchedTrades);
```

---

## 📊 運行流程圖

### 單次查詢流程

```
用戶查詢 (getDailyChipDataWithFallback)
    │
    ├─→ 驗證參數
    │
    ├─→ [嘗試方案 A] TWSE 爬蟲
    │   │
    │   ├─ ✅ 成功 → 返回結果
    │   │
    │   └─ ❌ 失敗（超時、403、網路錯誤等）
    │       │
    │       └─→ 日誌: ❌ [方案A失敗]
    │
    ├─→ [啟動方案 B] FinMind 備援
    │   │
    │   ├─ ✅ 成功
    │   │   │
    │   │   ├─→ 轉換格式 (convertFinMindToInternalFormat)
    │   │   │
    │   │   └─→ 返回結果
    │   │
    │   └─ ❌ 失敗
    │       │
    │       └─→ 日誌: 🚨 [方案B也失敗]
    │
    └─→ 返回空列表
```

---

## 🧪 測試檢查

已提供完整的測試用例 `InstitutionalServiceFallbackTest.java`，包含：

1. ✅ `testGetDailyChipDataWithFallback()` - 單日查詢
2. ✅ `testGetRecentInstitutionalTrades()` - 最近 N 天查詢
3. ✅ `testCalculateTrustLockScore()` - 評分計算
4. ✅ `testTWSEFailoverScenario()` - 容錯模擬
5. ✅ `testBatchQueryWithFallback()` - 批量查詢

**執行方式**：
```bash
mvn test -Dtest=InstitutionalServiceFallbackTest
```

---

## 🚨 常見問題排查

### Q1: 日誌出現 "⚠️ [方案A無資料]"，會切換嗎？

**A**: 是的。無數據也會觸發備援，自動查詢 FinMind。

```java
// InstitutionalService.java 第 76-77 行
logger.warning("⚠️  [方案A無資料] TWSE 沒有返回該日期的資料，嘗試備援...");
throw new Exception("TWSE 無資料");  // 觸發catch block
```

---

### Q2: FinMind API 配置錯誤會怎樣？

**A**: 會捕捉異常並記錄，最終返回空列表。

日誌示例：
```
🚨 [方案B也失敗] FinMind 備援通道也出現問題！
   FinMind 錯誤: IOException - 401 Unauthorized
⚠️  [完全失敗] 無法從任何數據源取得籌碼資料
```

---

### Q3: 資料格式轉換是否完全無損？

**A**: 不完全無損。FinMind 提供的是聚合的買賣超，未分類為外資/投信/自營商。

| 原始數據 | 轉換結果 | 說明 |
|---------|--------|------|
| FinMind buy | trustBuy | 映射為投信買超 |
| FinMind sell | dailyVolume 的一部分 | 用於計算交易量 |
| 無分類 | foreignBuy=0 | FinMind 不提供細分 |
| 無分類 | dealerBuy=0 | FinMind 不提供細分 |

---

### Q4: 可以同時查詢多個股票嗎？

**A**: 可以。建議使用批量查詢，容錯機制對每個股票獨立生效。

```java
for (String symbol : symbols) {
    List<InstitutionalTrade> data = institutionalService
        .getDailyChipDataWithFallback(symbol, date);
}
```

---

## 🎯 最佳實踐

### ✅ 推薦做法

1. **結合資料庫緩存**
   ```java
   // 優先查本地DB，然後再嘗試容錯機制
   List<InstitutionalTrade> cached = DatabaseManager.get(symbol, date);
   if (cached.isEmpty()) {
       cached = institutionalService.getDailyChipDataWithFallback(symbol, date);
   }
   ```

2. **非同步背景更新**
   ```java
   @Async
   public void updateInBackground(String symbol, String date) {
       institutionalService.getDailyChipDataWithFallback(symbol, date);
   }
   ```

3. **監控容錯頻率**
   ```java
   // 如果經常觸發方案B，表示TWSE不穩定，考慮告警
   metrics.incrementCounter("fallback.triggered");
   ```

### ❌ 避免做法

1. ❌ 不要在迴圈內高頻（超過 1 次/秒）查詢
2. ❌ 不要禁用異常捕捉以「偵測錯誤」
3. ❌ 不要假設 FinMind 數據 100% 準確
4. ❌ 不要在沒有緩存的情況下大量併發查詢

---

## 📈 性能預期

### 響應時間

| 場景 | 預期時間 | 備註 |
|------|---------|------|
| TWSE 成功 | 1-3 秒 | HTTP 客戶端請求 |
| TWSE 超時 | 15 秒 | 5秒超時 + 重試邏輯 |
| 切換到 FinMind | 2-4 秒 | API 直接調用 |
| 完全失敗 | < 20 秒 | 兩個超時加起來 |

### 資料庫存儲

已轉換的資料會自動存入本地 H2 資料庫，加速下次查詢。

---

## 📞 技術支持

如遇到問題，請檢查：

1. ✅ 日誌文件（`app.log`）中是否有錯誤記錄
2. ✅ FinMind API token 是否有效
3. ✅ 網路連接是否正常
4. ✅ Spring Boot 是否成功啟動


