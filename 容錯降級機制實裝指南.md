# 📋 自動容錯降級機制 - 實裝指南

> **核心功能**：當 TWSE 爬蟲因連線超時、403 封鎖或伺服器掛掉時，自動無縫切換到 FinMind 備援，確保系統 **永不停擺**！

---

## 🎯 核心架構

```
┌─────────────────────────────────────────────────────────────────┐
│                   InstitutionalService                          │
│                   (核心籌碼掃描服務)                              │
└──────────────────────┬──────────────────────────────────────────┘
                       │
        ┌──────────────┴──────────────┐
        │                             │
   🟢 PLAN A               🟠 PLAN B (Fallback)
   TWSE 官網               FinMind API
   (優先)                  (備援)
        │                             │
   ┌────▼────────┐           ┌───────▼──────┐
   │ TwseService │           │ FinMindClient│
   │ (爬蟲)      │           │ (API)        │
   └────┬────────┘           └───────┬──────┘
        │                             │
        └──────────────┬──────────────┘
                       ▼
        ┌──────────────────────────────┐
        │ convertInternalFormat()      │
        │ 格式轉換                      │
        └──────────────┬───────────────┘
                       ▼
        ┌──────────────────────────────┐
        │ InstitutionalTrade           │
        │ (系統統一格式)                 │
        └──────────────────────────────┘
```

---

## 📦 新增方法說明

### 1️⃣ `getDailyChipDataWithFallback()` - 每日籌碼資料（自動容錯）

**用途**：取得特定日期的籌碼資料，自動容錯降級

**簽名**：
```java
public List<InstitutionalTrade> getDailyChipDataWithFallback(String symbol, String date)
```

**參數**：
- `symbol`：股票代號（例如 "2330"）
- `date`：日期格式 "yyyy-MM-dd"（例如 "2024-05-19"）

**返回值**：
- `List<InstitutionalTrade>`：該日期的籌碼交易資料列表（已轉換為系統統一格式）

**工作流程**：
```
1. 驗證輸入參數
   ↓
2. [方案A] 嘗試從 TWSE 抓取
   ├─ ✅ 成功 → 返回結果
   └─ ❌ 失敗 ↓
3. [方案B] 自動切換到 FinMind API
   ├─ ✅ 成功 → 轉換格式 → 返回結果
   └─ ❌ 也失敗 ↓
4. 記錄警告，返回空列表
```

**使用範例**：
```java
@Autowired
private InstitutionalService institutionalService;

// 取得 2024-05-19 的 2330(台積電) 籌碼資料
List<InstitutionalTrade> trades = institutionalService
    .getDailyChipDataWithFallback("2330", "2024-05-19");

if (!trades.isEmpty()) {
    InstitutionalTrade trade = trades.get(0);
    System.out.println("投信買超: " + trade.getTrustBuy());
    System.out.println("外資買超: " + trade.getForeignBuy());
    System.out.println("自營商買超: " + trade.getDealerBuy());
}
```

---

### 2️⃣ `convertFinMindToInternalFormat()` - 格式轉換

**用途**：將 FinMind 的籌碼數據轉換為系統內部統一格式

**簽名**：
```java
private List<InstitutionalTrade> convertFinMindToInternalFormat(
    List<FinMindChipData> backupData
)
```

**轉換邏輯**：

| FinMind 欄位 | → | InstitutionalTrade 欄位 | 說明 |
|-------------|---|---------------------|------|
| `buy` | → | `trustBuy` | FinMind 的買超映射為投信買超 |
| `sell` | → | 計算入 `dailyVolume` | 用於計算日成交量 |
| `date` | → | `date` | 直接對應 |
| 無對應 | → | `foreignBuy = 0` | 未分類，設為0 |
| 無對應 | → | `dealerBuy = 0` | 未分類，設為0 |

**轉換結果範例**：
```
FinMindChipData:
  - date: "2024-05-19"
  - buy: 1,234,567
  - sell: 987,654

↓ 轉換 ↓

InstitutionalTrade:
  - date: "2024-05-19"
  - trustBuy: 1,234,567
  - foreignBuy: 0
  - dealerBuy: 0
  - dailyVolume: 2,222,221
```

---

### 3️⃣ `getRecentInstitutionalTrades()` - 改進版資料取得

**增強功能**：原方法已改進，失敗時自動啟用 FinMind 備援

```java
public List<InstitutionalTrade> getRecentInstitutionalTrades(
    String symbol, 
    int days
)
```

**工作流程**：
```
1. 檢查本地資料庫
   ├─ ✅ 有資料 → 返回
   └─ ❌ 無或過舊 ↓
2. [方案A] 從 TWSE 取得
   ├─ ✅ 成功 → 存入DB → 返回
   └─ ❌ 失敗 ↓
3. [方案B] 從 FinMind API 取得
   ├─ ✅ 成功 → 轉換 → 存入DB → 返回
   └─ ❌ 也失敗 ↓
4. 返回本地資料或空列表
```

**使用範例**：
```java
// 取得最近30天的投信籌碼資料
List<InstitutionalTrade> history = institutionalService
    .getRecentInstitutionalTrades("2330", 30);

// 計算投信鎖碼評分（自動使用上述方法）
int score = institutionalService.calculateTrustLockScore("2330");
```

---

## 🔄 新增的 FinMindClient 方法

### `fetchChipDataBackup()` - 備援機制入口

```java
public List<FinMindChipData> fetchChipDataBackup(
    String symbol, 
    String date
)
```

**功能**：
- 當 TWSE 失敗時調用
- 自動重試 2 次
- 返回 FinMind 的籌碼資料

**實現細節**：
```java
public List<FinMindChipData> fetchChipDataBackup(String symbol, String date) {
    logger.info("🔄 [FinMind 備援] 嘗試從 FinMind 備援通道獲取籌碼資料: " + symbol + " @ " + date);
    return fetchChipDataWithRetry(symbol, date, 2);  // 重試 2 次
}
```

---

## 🛡️ 容錯機制詳解

### 異常捕捉類型

```java
try {
    // TWSE 嘗試
} catch (Exception twseException) {
    // 捕捉所有異常：
    // - SocketTimeoutException: 連線超時
    // - HttpStatusException: 403 Forbidden
    // - IOException: 網路中斷
    // - 其他任何異常
}
```

### 日誌輸出範例

**成功場景**：
```
🔄 [方案A] 正在嘗試從 TWSE 抓取籌碼資料 [2330] @ 2024-05-19
✅ [方案A成功] TWSE 返回籌碼資料: trustBuy=1234567
```

**容錯降級**：
```
🔄 [方案A] 正在嘗試從 TWSE 抓取籌碼資料 [2330] @ 2024-05-19
❌ [方案A失敗] TWSE 伺服器報錯、超時或無資料！啟動防爆機制，切換至 FinMind 備援通道...
   原因: SocketTimeoutException - Connection timed out
🔄 [方案B] 正在嘗試從 FinMind API 抓取籌碼資料 [2330] @ 2024-05-19
✅ [方案B成功] FinMind API 返回 1 筆籌碼資料
✅ 成功轉換 1 筆 FinMind 資料為內部格式
```

**完全失敗**：
```
❌ [方案A失敗] TWSE 伺服器報錯...
🚨 [方案B也失敗] FinMind 備援通道也出現問題！
⚠️  [完全失敗] 無法從任何數據源取得籌碼資料
```

---

## 🚀 集成使用指南

### Controller 層使用範例

```java
@RestController
@RequestMapping("/api/institutional")
public class InstitutionalController {
    
    @Autowired
    private InstitutionalService institutionalService;
    
    /**
     * 取得某日籌碼資料（自動容錯）
     */
    @GetMapping("/daily-chip-data/{symbol}/{date}")
    public List<InstitutionalTrade> getDailyChipData(
            @PathVariable String symbol,
            @PathVariable String date) {
        return institutionalService.getDailyChipDataWithFallback(symbol, date);
    }
    
    /**
     * 取得最近N天籌碼資料（自動容錯）
     */
    @GetMapping("/recent/{symbol}/{days}")
    public List<InstitutionalTrade> getRecent(
            @PathVariable String symbol,
            @PathVariable int days) {
        return institutionalService.getRecentInstitutionalTrades(symbol, days);
    }
    
    /**
     * 計算投信鎖碼評分
     */
    @GetMapping("/trust-lock-score/{symbol}")
    public Integer getTrustLockScore(@PathVariable String symbol) {
        return institutionalService.calculateTrustLockScore(symbol);
    }
}
```

### 排程任務使用範例

```java
@Service
public class ScheduledDataFetchService {
    
    @Autowired
    private InstitutionalService institutionalService;
    
    /**
     * 每日自動更新籌碼資料（帶容錯機制）
     */
    @Scheduled(cron = "0 0 15 * * ?")  // 每日下午3點
    public void updateInstitutionalData() {
        List<String> symbols = List.of("2330", "2454", "3402", "2412");
        
        for (String symbol : symbols) {
            String today = LocalDate.now().toString();
            List<InstitutionalTrade> data = institutionalService
                .getDailyChipDataWithFallback(symbol, today);
            
            if (!data.isEmpty()) {
                System.out.println("✅ " + symbol + " 資料已更新");
            } else {
                System.out.println("⚠️  " + symbol + " 無法更新");
            }
        }
    }
}
```

---

## ⚙️ 配置參數

確保你的 `application.properties` 已配置：

```properties
# ── FinMind API ─────────────────────────────────────────
finmind.api.token=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...
finmind.api.url=https://api.finmindtrade.com/api/v4/data
```

RestTemplate 超時配置（已在 AppConfig 中設定）：
```java
@Bean
public RestTemplate restTemplate(RestTemplateBuilder builder) {
    return builder
            .setConnectTimeout(Duration.ofSeconds(5))
            .setReadTimeout(Duration.ofSeconds(10))
            .build();
}
```

---

## 📊 性能建議

### 緩存策略
```java
@Cacheable(value = "institutionalTrades", key = "#symbol + '-' + #date")
public List<InstitutionalTrade> getDailyChipDataWithFallback(String symbol, String date) {
    // ...
}
```

### 線程池配置（大量請求時）
```java
@Bean
public Executor taskExecutor() {
    ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
    executor.setCorePoolSize(5);
    executor.setMaxPoolSize(10);
    executor.setQueueCapacity(100);
    executor.initialize();
    return executor;
}
```

---

## 🔍 故障排查

### Q1: 日誌顯示 "TWSE 超時"，會自動切換嗎？
**A**：是的！任何異常（包括超時、403、網路中斷）都會觸發自動容錯降級到 FinMind。

### Q2: FinMind 也失敗怎麼辦？
**A**：系統會返回空列表，日誌會記錄警告。建議結合本地資料庫緩存使用。

### Q3: 如何禁用容錯機制，只用 TWSE？
**A**：直接呼叫 `twseService.fetchRecentInstitutionalData()` 即可。

### Q4: 資料轉換後是否準確？
**A**：FinMind 的 `buy/sell` 已映射為 `trustBuy`，但分類會有差異。建議在業務層進行驗證。

---

## 📝 版本歷史

| 版本 | 日期 | 變更 |
|------|------|------|
| 1.0 | 2024-05-19 | 初始實裝，核心容錯降級機制 |
| | | - 新增 getDailyChipDataWithFallback() |
| | | - 改進 getRecentInstitutionalTrades() 支持 FinMind 備援 |
| | | - 新增 convertFinMindToInternalFormat() 格式轉換 |

---

## 🎉 總結

✅ **自動容錯**：TWSE 失敗自動切換 FinMind  
✅ **無縫集成**：無需修改上層代碼  
✅ **詳細日誌**：清楚的故障追蹤  
✅ **格式統一**：多源數據統一為 InstitutionalTrade  
✅ **永不停擺**：系統高可用性保證


